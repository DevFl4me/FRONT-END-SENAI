algoritmo "Vetor"
var
teste,contador,contador2:inteiro
 numeros : vetor[1..3] de inteiro
inicio
para contador <- 1 ate 3 faca
  leia(numeros[contador])
fimPara

contador<-1

para contador2 <- 1 ate 3 faca
  escreval (numeros[contador2])
  escreval(numeros[2] + numeros[3])
fimPara
fimalgoritmo